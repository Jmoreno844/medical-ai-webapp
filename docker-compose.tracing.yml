# Jaeger all-in-one with OTLP HTTP on 4318 (UI on 16686).
# Usage: docker compose -f docker-compose.tracing.yml up -d
services:
  jaeger:
    image: jaegertracing/all-in-one:1.57
    ports:
      - "16686:16686"
      - "4318:4318"
    environment:
      - COLLECTOR_OTLP_ENABLED=true
