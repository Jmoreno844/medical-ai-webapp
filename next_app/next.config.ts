import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    eslint: {
        // To enable/disable ESLint during build
        ignoreDuringBuilds: true, // Set to false to enable
    },
    // .
};

export default nextConfig;
