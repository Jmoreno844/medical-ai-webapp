# Cloud Functions

This directory contains Cloud Functions for the Medical AI application.

## Environment Setup

The application supports three distinct environments:

### Local Development

- Uses Docker and file-based configuration (.env.local)
- Run locally for development and testing
- Uses local service account credentials from a key file

To start:
